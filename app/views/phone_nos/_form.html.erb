<%= form_for(@phone_no) do |f| %>
  <% if @phone_no.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_no.errors.count, "error") %> prohibited this phone_no from being saved:</h2>

      <ul>
      <% @phone_no.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone_type %><br>
    <%= f.text_field :phone_type %>
  </div>
  <div class="field">
    <%= f.label :phone_number %><br>
    <%= f.number_field :phone_number %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
